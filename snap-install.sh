#!/bin/bash

sudo snap install btop
sudo snap install code --classic
sudo snap install cpufetch
sudo snap install gifski --classic
sudo snap install onefetch --classic
sudo snap install spectacle --classic
